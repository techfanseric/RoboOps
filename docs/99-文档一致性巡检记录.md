# RoboOps 文档一致性巡检记录

整理日期：2026-07-05
巡检范围：`00-项目索引.md` 至 `05-组织角色与权限模型.md`

## 1. 巡检结论

本次从头到尾复核后，文档主线已经统一：

- RoboOps 是机器人商业运营平台，不是茶饮后台。
- 饮品亭/咖啡亭是第一批样板场景，不是产品边界。
- 本期 MVP 目标是搭可配置框架，并跑通一个样板场景。
- 甲方大概率缺少完整商业运营团队，因此系统需要给出默认运营模型、角色模板、权限模板和异常分派方案。
- 不再把关键运营设计问题原样抛给甲方；后续向甲方/内部拿的是事实资料、配置选择和实施约束。

巡检后，没有发现会推翻产品方向的根本冲突。已修正的主要问题集中在 MVP 边界、角色权限粒度、待确认问题口径和后续文档编号。

## 2. 已发现并修正的问题

| 问题 | 原表现 | 处理方式 |
| --- | --- | --- |
| `02` 原口径偏“向甲方提问” | 一些问题默认等甲方回答，但甲方缺少经验 | 已改为“已知判断、自研假设、事实资料”三类 |
| `01` MVP 范围偏早期草案 | 更像订单/设备/退款后台，缺少场景模板、配置发布、权限包等 | 已补齐与 `04` 一致的 MVP 必做项 |
| `01` 角色权限过粗 | 只列老板、运营、客服、运维、财务等 | 已注明由 `05` 扩展为角色族、权限包、数据范围、审批策略 |
| `04` 多租户/多品牌边界不够清楚 | 既说要租户/品牌，又可能被理解为第一版要完整 SaaS | 已明确模型预留，MVP 界面可先少量客户/品牌 |
| `03` 权限参考过粗 | 只提平台管理员、客户管理员、运营等基础角色 | 已补充应参考 `05` 的角色模板和权限包模型 |
| `04`、`05` 的“待确认”容易误解 | 容易被理解成等待甲方定义产品 | 已改为“事实验证与配置选择” |
| 后续文档编号不统一 | 角色权限文档加入后，PRD 编号需要后移 | 已统一为 `06-PRD-v0.1.md`、`07-信息架构与页面清单.md`、`08-核心数据模型ERD.md`、`09-MVP原型与演示数据方案.md` |

## 3. 当前统一术语

| 建议术语 | 避免写死的语义 | 说明 |
| --- | --- | --- |
| 点位/场景单元 | 门店 | 可覆盖亭子、展点、服务站、柜体、工作站 |
| 商品/服务 | 饮品 | 可覆盖饮品、零售商品、机器人服务、预约服务 |
| 订单/服务请求 | 饮品订单 | 可覆盖交易订单、服务单、任务请求 |
| 履约状态机 | 制作流程 | 可覆盖制作、递送、讲解、服务执行 |
| 异常/事件 | 故障告警 | 异常要进入分派、处理、关闭闭环 |
| 任务/工单 | 运维单 | 可覆盖补料、巡检、维修、人工确认、客服处理 |
| 资源/耗材 | 物料/料仓 | 饮品场景可展开为原料和库存，其他场景可表达耗材 |
| 配置发布 | 模板下发 | 覆盖商品、点位、设备、场景、状态、异常规则的发布 |

## 4. 当前统一 MVP 边界

### 4.1 必须纳入

- 租户/客户边界、品牌、组织、角色基础框架。
- 场景模板，至少包含饮品亭和通用机器人服务站两个样板。
- 点位管理和点位绑定机器人/设备。
- 机器人/设备台账、在线状态、运行状态、事件日志。
- 商品/服务目录，支持自定义属性、规格/SKU、上下架、适用点位。
- 订单/服务请求列表、详情、生命周期状态。
- 轻量状态机，支持底层状态和场景显示名。
- 异常中心，支持异常生成、分派、处理、关闭。
- 配置发布记录。
- 工作台看板。
- 用户、角色模板、权限包、数据范围、基础审批策略。
- 操作日志和高风险动作留痕。

### 4.2 可以简化

- 多租户：模型保留，第一版不做完整 SaaS 商业化能力。
- 多品牌：模型保留，界面可先开放单品牌或少量品牌。
- 财务：先做收入、退款、导出占位，不做完整结算/分账。
- 工单：先做异常处理记录和轻量任务，不做完整工单系统。
- 配置发布：先做记录、范围、版本和状态，不做复杂灰度/回滚。
- 小程序：先打通订单/支付/状态，不把消费者端完整产品纳入后台 MVP。

### 4.3 暂不纳入

- 完整配方引擎。
- 完整库存、仓储、采购、补货。
- 完整财务结算、分账、对账。
- 完整 OTA、远程控制、远程调试。
- 复杂工作流引擎。
- 会员、营销、CRM。
- 高级 BI 和自定义报表设计器。
- 多机器人空间调度。
- 完整客服系统。

## 5. 当前统一角色权限口径

角色权限不是固定岗位表，而是可配置模型：

> 用户 + 角色模板/角色实例 + 权限包 + 数据范围 + 审批策略 + 异常分派规则 + 审计日志

MVP 至少应内置这些角色模板：

- 平台支持。
- 租户管理员。
- 业务负责人。
- 运营负责人。
- 点位负责人。
- 商品/配置管理员。
- 配置发布人。
- 客服/售后。
- 退款审批人。
- 机器人/设备运维。
- 现场维护员。
- 财务/结算。
- 数据查看员。
- 审计员。
- 演示操作员。

系统应支持客户一人多岗，但必须显性化职责边界，尤其是：

- 退款发起和退款审批尽量分离。
- 配置编辑和配置发布尽量分离。
- 设备高风险命令需要二次确认和审计。
- 外部供应商只能看到对应点位、设备或工单。
- 每类异常必须有默认负责人。

## 6. 当前仍缺的关键细节

以下不是文档冲突，而是后续 PRD/设计阶段需要继续补齐的内容。

### 6.1 事实资料

- 现有 API 文档。
- 小程序下单链路。
- 支付状态回调。
- 订单状态字段。
- 设备/机器人状态字段。
- 错误码/异常码。
- 真实异常案例。
- 深圳试点点位和设备组合。
- 当前支付、退款、客服、现场维护责任主体。
- 两个月窗口期的准确里程碑和工程资源。

### 6.2 产品设计稿

- `06-PRD-v0.1.md`。
- `07-信息架构与页面清单.md`。
- `08-核心数据模型ERD.md`。
- `09-MVP原型与演示数据方案.md`。

### 6.3 需要进一步细化的设计问题

- 订单/服务请求底层表最终命名：`orders`、`service_requests` 还是 `business_requests`。
- 场景模板字段配置的第一版粒度。
- 配置发布是否需要第一版审批。
- 高风险设备命令的第一版范围。
- Demo 是否面向展会、样板间还是客户沟通。
- RoboOps 第一版是平台代运营优先，还是客户自助使用优先。

## 7. 后续写 PRD 的约束

后续 `06-PRD-v0.1.md` 应遵守以下约束：

- 不再写成饮品亭后台 PRD。
- 不再把角色写死为固定岗位。
- 不再把客户无法回答的问题作为 PRD 前置条件。
- 所有饮品语义必须落在“饮品亭场景模板”里，不能进入核心模型。
- PRD 必须包含团队搭建向导、角色模板、权限包、异常分派规则。
- PRD 必须明确哪些是 MVP 必做、哪些只是扩展位、哪些明确暂不做。

