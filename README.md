# RoboOps

当前版本：`v0.2.1`

RoboOps 是面向机器人、自动化设备与具身智能商业落地场景的商业运营平台。它把点位、设备/机器人、商品/服务流程、订单/服务请求、支付、异常、任务工单、角色权限、配置发布和数据看板串成一套可复制的运营系统。

当前仓库包含 RoboOps 的产品规划文档、项目材料归档规范，以及一个基于 Vite + React + TypeScript 的前端 MVP 工程。MVP 重点验证运营后台的信息架构、角色权限、配置发布、异常处理、任务工单、退款售后和报表闭环。

## 在线演示

生产地址：

```text
https://roboops-console.pages.dev/
```

GitHub 仓库：

```text
https://github.com/techfanseric/RoboOps
```

演示登录：

```text
密码：RoboOps@2026
```

可使用启用账号的姓名、账号 ID 或本地邮箱登录。常用示例：

| 账号 | 角色 | 数据范围 |
| --- | --- | --- |
| 林砚 / `usr-000` | 平台支持 | RoboOps 平台 / 全部租户 |
| 周岚 / `usr-006` | 租户管理员 | 星舟具身智能 / 全部品牌 |
| 李明 / `usr-001` | 业务负责人 | 星舟具身智能 / 全部品牌 |
| 陈澄 / `usr-002` | 运营负责人 | RoboTea Lab / 深圳 |
| 王越 / `usr-003` | 机器人/设备运维 | 深圳湾展厅 A 点、南山机器人饮品亭 |
| 程川 / `usr-012` | 现场维护员 | 南山机器人饮品亭 |
| 顾凡 / `usr-013` | 数据查看员 | RoboTea Lab / 深圳 |
| 叶青 / `usr-014` | 审计员 | 星舟具身智能 / 全部品牌 / 审计数据 |
| 方衡 / `usr-020` | 集成管理员 | 星舟具身智能 / 全部品牌 |
| 许工 / `usr-021` | 开发者 | 星舟具身智能 / 现场运营助手 |

完整账号和角色视角见 `docs/15-登录账号与角色视角矩阵.md`。

## 当前能力

- 工作台：按当前账号职责展示经营状态、优先队列和处理边界。
- 品牌或组织：管理租户、品牌、组织骨架和运营范围。
- 场景模板：抽象饮品亭、机器人服务站等业务对象、字段、状态和异常类型。
- 点位管理：维护点位营业状态、上线检查、可售范围和健康状态。
- 机器人/设备管理：查看设备状态、事件日志、命令记录和高风险命令审批。
- 商品/服务目录：管理 SKU、服务项目、可售点位和配置变更。
- 规格、配方与资源耗材：支持规格模板、配方版本与范围冲突、工艺步骤、料仓、效期方案、当日批次、报损和打印记录。
- 设备运维：支持硬件能力、配置模板、料仓标定、离线策略、维护方案、软件包三态与批量升级策略。
- 订单/服务请求中心：支持点位资料、手工单防重、履约前置校验、首次下发、显式重试、执行与打印记录。
- 状态与异常中心：集中查看设备健康与四类业务状态，维护状态字典和异常规则，并承接异常推进、转任务、退款、关闭与审计留痕。
- 任务/工单：支持现场巡检、维修、补给、人工确认等任务闭环。
- 配置发布：支持对象级配置发布、审批、回退申请和发布影响范围。
- 报表：提供订单、支付、商品、物料、损耗、设备、点位等细分报表，以及可追踪的异步导出任务。
- 角色权限：验证菜单权限、动作权限、数据范围、角色实例和审批分离。
- 平台与开放能力：管理企业、平台账号、菜单、默认设置、异步任务、开发者审批、企业授权、OpenAPI/MQTT 凭证、事件订阅和数据同步。

## v0.2.1 业务更新清单：状态与异常中心

本版本将原“异常中心”升级为“状态与异常中心”，集中补充机器人与设备状态的查看、配置和发布能力。既有异常处置流程保持不变，新增能力继续作为前端业务仿真，为后续真实设备和 API 接入预留边界。

### 1. 状态与异常中心

- 原“异常中心”升级为“状态与异常中心”，集中提供实时状态总览、异常事件、状态字典、异常规则和发布记录。
- 按连接、运行、业务和安全四个维度展示设备状态，并关联设备健康、状态时间线、当前异常和既有设备命令。
- 状态字典和异常规则支持业务影响、负责人、SLA、处理指引、通知、恢复方式，以及平台至单台设备的分层作用范围。
- 配置变更按照提交、非本人审批和发布流程生效，记录影响范围与逐设备结果，并支持失败重试和版本回退。
- 异常静默、配置、审批、发布和只读权限继续受到企业、点位、设备范围及角色职责约束。
- 原异常登记、分派、任务、退款和关闭流程保持不变；新增状态、回执和设备联动仍为前端仿真，不代表已接入真实设备或后端服务。

## v0.2.0 业务更新清单

本版本以甲方旧后台源码为业务规则基线，将已有且成熟的能力补充到 RoboOps 现有信息架构中。页面布局没有机械复制旧后台，但业务规则、状态流转、权限边界和异常处理均按原业务逻辑完成映射。以下为本版本正式记录的十七项更新。

### 1. 整体导航与业务入口

- 新增“资源耗材”“设备运营”“订单运营”“高级报表”“平台管理”和“集成开放”六个业务入口。
- 设备列表、订单请求、报表和角色权限页面增加对应的深层业务入口，避免新增功能只能通过地址访问。
- 工作台与侧边导航同步补充模块入口，并继续根据当前角色、企业和点位范围控制可见性。
- 平台管理入口仅向平台支持人员展示，开发者只看到与本人接入资产相关的开放能力。

### 2. 商品规格、执行配方与工艺

- 支持规格组、规格项、别名、排序和启停，以及由多个规格组组成的规格模板。
- 配方按照“商品＋规格组合”生成组合编码，同一组合按版本递增保留历史。
- 全国、区域和点位配方发生范围冲突时，停用冲突旧版本并清除旧执行步骤；局部点位管理员不能创建影响授权范围外的区域或全国配方。
- 配方必须引用真实且启用的规格、物料、单位和工艺；任一错误步骤都会导致整份配方拒绝保存。
- 配方下发按点位分别记录成功、失败及原因，只允许重试失败点位。
- 工艺方案支持步骤顺序、转速、方向和持续时间，只有启用工艺可以参与订单履约。

### 3. 物料、计量单位与料仓

- 支持计量单位名称、编码和精度管理，名称与编码保持唯一。
- 物料资料增加单位、密度、保存方式、兼容硬件、有效期、预警时间和标定精度。
- 支持常温、冷藏、冷冻、热保存、其他和保温桶冷藏等保存类型。
- 料仓支持补料、出料、容量调整和低余量预警，并记录补料时间与到期时间。
- 补料不得超过容量，出料不得超过余量，新容量不得低于已有余量，业务数量必须是正整数。
- 点位人员只能查看和操作授权点位内的料仓与现场记录。

### 4. 效期方案、批次与现场作业

- 支持效期方案新增、编辑、删除、自动报损开关以及按物料配置有效时间和预警时间。
- 同一时间一个点位只能绑定一套效期方案；局部编辑方案时保留其他授权范围外点位的既有绑定。
- 支持 `-1` 表示物料在启用当日 `23:59:59` 到期。
- 批次只允许录入当天启用时间，同一点位、同一物料、同一启用时刻禁止重复录入。
- 批次支持可用、临期、过期和已报损状态，并自动计算预警与到期时间。
- 打印区分首次打印和补打；补打不得超过首次打印重量，过期或已报损批次禁止打印。
- 人工报损必须填写原因且不能超过可用量；发生部分报损后整个批次退出可用和打印状态。
- 启用自动报损的方案在当天 06:00 后处理符合条件的过期批次并写入记录。

### 5. 设备纳管与硬件能力

- 设备资料补充企业、点位、供应商、硬件型号、联网方式及屏幕、打印、扫码、称重和制冷等能力。
- SN 只允许字母或数字，唯一性校验不区分大小写。
- 设备禁止跨企业或跨点位直接改绑；设备型号必须来自已获当前企业授权的供应商。
- 绑定点位必须真实存在并属于当前企业，空点位授权范围不会被解释为企业全部点位。
- 设备激活前执行完整资料校验，激活后继承企业默认离线策略并保留激活记录。

### 6. 设备配置、料仓标定与离线策略

- 配置模板按硬件型号管理参数，并在下发前检查模板与设备型号兼容性。
- 批量下发形成独立批次，每台设备分别记录结果，失败设备可以单独重试。
- 区分平台期望配置和设备实际应用配置；失败回执不会被错误标记为已经应用。
- 设备料仓支持容量、余量、阈值、补料、出料和容量调整，并形成完整操作日志。
- 标定记录保存标定前后数值、变化量、操作人和时间，并进入高级报表。
- 每个企业只能存在一个默认离线策略，同一设备只能绑定一个策略。

### 7. 维护方案、发布与实际维护

- 支持维护或清洗方案、适用设备、维护类型和有序执行步骤。
- 同一设备、同一维护类型只能存在一套有效方案，空步骤方案不能发布。
- 发布过程区分发布中、发布完成、部分失败和超时失败，并按设备汇总回执。
- 失败设备支持独立重试；删除方案不会删除历史发布记录。
- 新增实际维护记录，单独保存执行设备、维护类型、执行人员、完成时间、结果和说明。
- 方案下发与现场实际执行分别留痕，避免把“配置已发布”误判为“维护已完成”。

### 8. 软件包与设备升级策略

- 软件包分为 App、固件和 Web 三类，支持审核中、启用和停用状态。
- 相同类型、相同 MD5 可以复用已有文件地址，不同软件类型禁止错误复用。
- 存在其他版本依赖时禁止删除软件包，删除和状态变更需要确认及审计。
- 升级策略可选择目标版本、点位和设备，并保存发布范围快照。
- 升级策略最多十条，启用后锁定编辑。
- 升级过程区分下发中、成功、失败和超时；只有设备上报目标版本才判定成功，失败设备可以重试。

### 9. 点位扩展资料

- 点位资料增加平台点位编码、第三方点位编码、省市区、详细地址、经纬度和营业状态。
- 点位名称、平台编码和第三方编码在同一企业内保持唯一，不同企业可以复用相同编码。
- 上游点位信息刷新时，保留本地维护的编码、地址、坐标以及新建点位资料。
- 点位资料继续受到当前企业和授权点位范围控制。

### 10. 手工订单与履约下发

- 支持选择点位、商品规格、商品序号和数量创建手工订单，并自动生成订单号和取餐号。
- 对同一用户的秒级重复提交进行拦截，避免重复订单。
- 创建前检查点位营业状态、可用设备、启用配方、物料步骤和启用工艺。
- 下发前再次读取当前最新配方和工艺，并重新验证点位与设备状态。
- 首次下发只能执行一次，失败后必须使用明确的重试动作。
- 每次下发与重试分别记录设备 SN、结果、失败原因、时间和重试次数。
- 退单增加状态限制和幂等控制，成功退单不能无限重复发送，每次尝试独立留痕。

### 11. 生产过程与打印记录

- 订单详情展示物料步骤、预期用量、实际用量、完成状态、异常原因和执行耗时。
- 展示工艺动作、步骤顺序、转速和方向，支持生产执行过程追踪。
- 保留甲方旧后台的订单状态统计口径，包括待处理、已下发、制作中、已完成、失败和取消/退单。
- 订单打印区分首次打印和补打，并记录操作人、时间、结果和失败原因。
- 不存在的订单不能生成生产或打印记录。

### 12. 七类高级报表

- 新增商品销售、点位销售、生产明细、物料用量、料仓用量、标定记录和损耗记录七类报表。
- 支持今日、近 7 天和近 30 天时间范围，以及点位和报表类型筛选。
- 报表由订单、点位、配方、物料、料仓、批次、标定和损耗事实聚合生成。
- 商品、物料和料仓报表携带真实点位范围，并按当前授权点位求交集展示。
- 每条报表记录包含业务维度、数量、单位、发生时间和明细说明。

### 13. 报表与异步导出任务

- 提交导出前进行权限判断和二次确认，并保存报表类型与筛选条件。
- 任务列表最多展示最近 50 条，区分待执行、执行中、成功和失败。
- 超过 5 分钟无进度的任务自动转为失败。
- 执行中的任务不能标记已读或清理，终态任务可以标记已读和清理。
- 导出、已读和清理操作进入模块日志与主系统审计。

### 14. 平台管理

- 平台支持人员可以管理企业启停、平台账号、菜单策略、企业默认设置、平台日志和异步任务。
- 企业与账号启停由主系统再次校验平台权限，禁止停用当前登录账号。
- 已停用或不存在的账号不能继续恢复旧登录会话。
- 菜单策略可以维护路径、权限标识、可见性和启用状态。
- 企业默认设置包含效期系统、自动报损、报损拍照、默认通知渠道和各类编码前缀。
- 企业、账号、菜单、默认设置和任务清理等高风险动作均需二次确认，并写入主审计。
- 平台异步任务同样执行最近 50 条、5 分钟超时、处理中禁清理等规则。

### 15. 开发者与开放集成

- 支持开发者接入申请、权限范围审核、通过与拒绝；权限范围为空时不能审批通过。
- 企业授权同时受到企业、点位、权限、启用状态和有效期限制。
- OpenAPI 只展示掩码密钥，支持调用配额、限流状态、凭证版本和轮换。
- MQTT 凭证绑定主题前缀和点位范围，禁止跨企业订阅。
- 事件订阅支持回调地址、事件列表、连续失败次数、暂停恢复和签名轮换。
- OpenAPI、MQTT 和事件签名轮换后保留 15 分钟旧版本过渡期，到期自动清理。
- 数据同步任务增加所属企业、导入导出方向、进度、结果、失败重试和清理，并按企业范围隔离。
- 开发者只看到和管理本人申请、凭证及订阅，不能审批申请、管理企业授权或查看企业全局同步任务。

### 16. 权限、数据范围与审计

- 所有新增模块先按企业隔离，再按当前账号授权点位投影视图和业务对象。
- 审计员、数据查看人员和财务角色保持只读或限定查看权限。
- 现场角色只处理料仓、批次、标定和实际维护等现场动作。
- 配置管理员负责规格、配方、工艺和资源配置，设备运维负责设备、模板、维护和升级。
- 开发者只能管理本人集成资产，企业级凭证和同步操作要求明确的企业级数据范围。
- 高风险动作统一执行权限检查、二次确认，并记录成功、失败或取消结果。
- 审计内容包含操作人、业务对象、风险等级、结果和详细说明，模块日志同时回传主系统审计。

### 17. 跨角色状态共享与跨模块业务联动

- 同一企业不同角色共享资源、设备、订单和集成领域事实，页面再按各自角色和点位范围裁剪。
- 商品管理员创建的新配方可以被订单操作员读取，订单下发始终使用当前最新启用配方和工艺。
- 设备运维激活的设备进入订单选机和履约前置判断。
- 料仓、批次、标定和损耗事实进入七类高级报表。
- 集成管理员的审批和凭证轮换会反映到开发者本人视图，并保持单一凭证版本事实。
- 切换企业、品牌、场景或点位时重新加载对应数据范围，避免旧企业或旧点位数据写入新分区。
- 点位空授权范围严格表示没有数据权限，不会退化成查看企业全部数据。

本版本的完整实施与验收记录见 `docs/19-旧后台能力补全实施记录.md` 和 `docs/20-旧后台能力补全最终变更总账.md`。

## 本地运行

前端工程位于 `app/`：

```bash
cd /Users/ericyim/RoboOps/app
npm install
npm run dev
```

默认开发地址：

```text
http://127.0.0.1:5173/
```

常用命令：

```bash
cd /Users/ericyim/RoboOps/app
npm run check
npm test
npm run build
npm run preview
```

更详细的前端工程说明见 `app/README.md`。

## 仓库结构

```text
RoboOps/
  app/        React/Vite 前端工程
  docs/       产品规划、验收记录、权限模型、部署记录
  materials/  原始访谈材料、转录和总结归档
```

关键入口：

- `docs/00-项目索引.md`：项目统一口径、文档地图和阅读顺序。
- `docs/07-PRD-v0.1.md`：MVP 产品需求文档。
- `docs/08-信息架构与页面清单.md`：后台菜单、页面和关键字段。
- `docs/09-核心数据模型ERD.md`：核心实体、状态枚举和权限关系。
- `docs/13-ReactVite工程验收对照.md`：当前前端工程验收记录。
- `docs/18-免费云部署与CICD实施.md`：Cloudflare Pages + GitHub Actions 部署记录。
- `app/src/data/mockData.ts`：当前 MVP 的本地样例数据。
- `app/src/services/operations.ts`：前端样例业务动作、权限策略和状态变更逻辑。

## 技术栈

- Vite `6.4.3`
- React `19.2.7`
- TypeScript `6.0.3`
- React Router `7.18.1`
- Lucide React `1.23.0`
- Cloudflare Pages
- GitHub Actions

## 部署与 CI/CD

当前使用免费云资源部署：

- 静态托管：Cloudflare Pages
- 生产域名：`https://roboops-console.pages.dev/`
- 生产分支：`main`
- 构建目录：`app/dist`
- 自动部署：推送 `main` 后触发 `.github/workflows/deploy.yml`

GitHub Actions 需要以下仓库 Secrets：

```text
CLOUDFLARE_ACCOUNT_ID
CLOUDFLARE_API_TOKEN
```

不要将 Cloudflare API Token、数据库密码或其他敏感信息提交到仓库。

## 当前边界

当前版本是可交互的前端业务仿真与 API-ready 领域层，不是完整生产系统：

- 登录、权限、审批和审计使用前端样例数据验证产品闭环。
- 业务状态按租户保存在浏览器 `localStorage`，同租户不同角色共享领域事实、再按各自点位范围裁剪视图；刷新后可保留本机状态，但不同设备之间不会同步。
- 尚未接入真实 API、企业账号/SSO、服务端权限矩阵、服务端持久化和不可篡改审计日志。
- 当前不需要 Supabase；后续若要做多设备同步、真实账号、审计日志或 API outbox 持久化，再选择 Supabase Free、Cloudflare D1/KV/R2 或正式后端服务。
- 本次补齐的旧后台能力遵循原业务校验和状态流转，但 MQTT、Kafka、OSS、通知渠道与打印机均为界面内仿真，没有连接真实基础设施。

## 后续方向

- 接入真实认证和服务端 IAM。
- 将配置发布、审批、任务、异常、退款和 API outbox 持久化。
- 建立服务端权限矩阵和 API 二次校验。
- 补充真实设备、订单、支付、退款和文件附件接口。
- 增加路由级拆包，降低前端首包体积。
- 增加部署监控、失败通知和线上访问分析。
